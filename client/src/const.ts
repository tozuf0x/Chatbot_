export enum AppRoute {
  Root = '/',
  Auth = '/auth',
  NotFound = '*'
}

export enum APIRoute {
  Guidances = 'guidances',
}

export enum Mode {
  Idle = 'idle',
  Add = 'add',
  Edit = 'edit',
  Delete = 'delete'
}

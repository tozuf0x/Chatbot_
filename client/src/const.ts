export enum AppRoute {
  Root = '/',
  Auth = '/auth',
  NotFound = '*'
}

export enum APIRoute {
  Recommendations = 'recommendations',
}

export enum Mode {
  Idle = 'idle',
  Add = 'add',
  Edit = 'edit',
  Delete = 'delete'
}
